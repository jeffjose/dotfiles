# appimage catalog — apps installable via `appimage install -i` or `appimage install <name>`.
#
# Tab-separated columns:  name <TAB> source_url <TAB> guard <TAB> description
#                         [<TAB> flags]
#   - guard: name of an appimage-guards/<guard>.sh hook, or "-" for none
#   - flags: optional 5th column, comma-separated. Last on purpose, so the rows
#     without it keep parsing exactly as before. Understood values:
#       cli         the app is a CLI as well as a GUI, so the wrapper runs it in
#                   the foreground with output on the terminal when given
#                   arguments and detaches only when launched bare
#       no-sandbox  replay the sandbox switches from the app's own .desktop in
#                   the wrapper. Only for apps that will not start otherwise —
#                   it turns Chromium's sandbox off. Plenty of the Electron
#                   AppImages here ship --no-sandbox and run fine sandboxed
#                   anyway, so this is never applied automatically.
#   - source_url is a GitHub repo/release URL (preferred, enables updates), a
#     templated download URL, a "latest" URL marked with a trailing #latest, or
#     a direct .AppImage URL (the last of these gets no updates)
#   - lines starting with # and blank lines are ignored
#
# Templated source_url: for projects that tag releases on GitHub but host the
# AppImage themselves, write the download URL with placeholders and append
# "#github=OWNER/REPO" to say where the version comes from. Placeholders:
# {tag} (v5.0.0), {version} (5.0.0), {arch} (x86_64/aarch64), {arch_short}
# (x64/arm64). Never pin a version here — it re-resolves on every update.
#
# Keep columns tab-separated (not spaces). Every field must be non-empty (use
# "-" for no guard) — adjacent tabs collapse when parsed and would misalign.
#
# code — dropped in favour of the apt package. VS Code ships a real .deb, and the
# AppImage costs ~200MB extra: the squashfuse daemon plus the compressed image
# sitting in page cache on top of the decompressed pages it serves. AppRun also
# leaks its mountpoint into PATH/LD_LIBRARY_PATH/XDG_DATA_DIRS, which the
# integrated terminal then inherits (and the path goes stale on every restart).
# Kept here for reference only:
# code	https://github.com/valicm/VSCode-AppImage	-	VS Code (official build, repackaged as an AppImage)
claude-desktop	https://github.com/aaddrick/claude-desktop-debian	personal-only	Claude Desktop (community Linux build; personal hosts only)
lmstudio	https://lmstudio.ai/download/latest/linux/x64?format=AppImage#latest	personal-only	LM Studio (local LLM runner; personal hosts only). Not on GitHub, so the version comes from where the vendor's "latest" URL redirects — the old entry pinned .../0.4.19-2/LM-Studio-0.4.19-2-x64.AppImage and could never update.
discord	https://github.com/pkgforge-dev/Discord-AppImage	personal-only	Discord (unofficial community build by pkgforge-dev; personal hosts only)
croc-gui	https://github.com/interfluve-wav/croc-gui	-	Croc GUI (desktop front-end for the croc CLI; named croc-gui so it does not collide with the croc CLI from mise)
t3-code	https://github.com/pingdotgg/t3code	-	T3 Code (t3.codes — desktop control plane for driving Claude Code, Codex, OpenCode et al from one window; the repo also cuts nightly prereleases, but /releases/latest keeps us on stable)
sonic-pi	https://sonic-pi.net/files/releases/{tag}/Sonic-Pi-for-Linux-{arch_short}-{tag}.AppImage#github=sonic-pi-net/sonic-pi	-	Sonic Pi (live-coding music environment; self-hosted download, version tracked from GitHub releases)
opencode-desktop	https://github.com/sst/opencode	-	OpenCode Desktop (GUI for the opencode agent; named opencode-desktop so it does not shadow the opencode CLI installed from mise as npm:opencode-ai — ~/bin sorts ahead of the mise shims)
paseo	https://github.com/getpaseo/paseo	-	Paseo (orchestrator for Claude Code / Codex / OpenCode across desktop, mobile and web). Ships one binary that is both the Electron app and the `paseo ls|run|status|logs` CLI, hence the cli flag. Its own .desktop is "Exec=AppRun --no-sandbox %U" — Electron 41 / Chrome 146 cannot start the namespace sandbox here and the setuid fallback can never work from a squashfs AppImage, so without that flag it dies on a /dev/shm error before painting a window.	cli,no-sandbox
